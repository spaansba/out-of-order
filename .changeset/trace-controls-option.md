---
"@out-of-order/trace": minor
---

`trace()` accepts `controls: false` to skip the floating in-page control panel while keeping the overlay and the peek key active, for hosts that bring their own controls (like the browser extension's side panel).
