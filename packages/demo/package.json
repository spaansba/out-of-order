{
  "name": "@out-of-order/demo",
  "version": "0.0.0",
  "private": true,
  "description": "Interactive demo for Out of Order: edge cases + a deliberately broken page.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@out-of-order/core": "workspace:*",
    "@out-of-order/trace": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
